import React from 'react'
import ReactDOM from 'react-dom/client'
import { UserSegmentationApp } from './UserSegmentationApp'
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <UserSegmentationApp />
  </React.StrictMode>,
);
